export default function Footer() {
  return (
    <footer className="border-t border-[var(--color-border)] py-8">
      <div className="shell flex flex-col gap-3 text-sm text-[var(--color-copy)] sm:flex-row sm:items-center sm:justify-between">
        <p className="display-heading text-lg text-[var(--color-terracotta)]">
          Zawlbuk
        </p>
        <p>
          Zawlbuk — Learn local. Work global. | A Mizoram Government Initiative
        </p>
      </div>
    </footer>
  );
}
